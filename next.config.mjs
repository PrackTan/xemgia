/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['bachlongmobile.com',"beta-api.bachlongmobile.com","static-alepay.nganluong.vn"], // Add the domain here
      },    
};

export default nextConfig;
